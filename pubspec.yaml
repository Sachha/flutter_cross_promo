name: cross_promo
description: Cross-promote your Flutter apps to each other's users. Theme-adaptive widgets that integrate naturally into any app.
version: 1.0.0

environment:
  sdk: ^3.11.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.2.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  assets:
    - assets/
